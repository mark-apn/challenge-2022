import 'package:grpc/grpc_connection_interface.dart';

ClientChannelBase createNewGrpcChannel(
  bool local,
  bool secure,
  String host,
  int port,
) {
  throw UnimplementedError('Cannot create from stub');
}
